version: '3.8'

services:
  game:
    build:
      context: ./src
      dockerfile: Dockerfile
    container_name: rpg_game
    ports:
      - "5173:5173"
    environment:
      - VITE_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ./src:/app
    command: npm run dev -- --host 0.0.0.0